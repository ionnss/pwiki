package main

/*import (
	"bufio"
	"fmt"
	"log"
	"os"
)

func main() {
	// ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
	// abrir arquivo
	// ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
	arquivo, err := os.Open("ATD/00psico.txt")
	if err != nil {
		log.Fatalf("Erro ao abrir o arquivo: %s", err)
	}
	defer arquivo.Close()

	// ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
	// ler arquivo com buffer (scanner)
	// ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
	scan := bufio.NewScanner(arquivo) // cria um novo leitor do arquivo

	contador := 0 // variável para contagem de linhas

	var linhas []string // variável para armazenar linhas

	for scan.Scan() { // loop para leitura do arquivo
		linha := scan.Text()                           // leitura da linha
		contador++                                     // incrementa contagem de linhas
		fmt.Printf("Linha %v : %v\n", contador, linha) // exibe linha lida e contagem de linhas
		linhaModificada := fmt.Sprintf("Linha %d: %s", contador, linha)
		linhas = append(linhas, linhaModificada)

	}

	// ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
	// tratamento de erro durante leitura do arquivo
	// ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
	if err := scan.Err(); err != nil {
		log.Fatalf("Erro ao ler o arquivo: %s", err)
	}

	// ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
	// criar arquivo saida
	// ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
	saida, err := os.Create("ATD/00saida.txt")
	if err != nil {
		log.Fatalf("Erro ao criar o arquivo: %s", err)
	}
	defer saida.Close()

	// ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
	// escrever no arquivo
	// ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
	escritor := bufio.NewWriter(saida)
	for _, linha := range linhas {
		fmt.Fprintln(escritor, linha)
	}

	escritor.Flush() // escreve as linhas no arquivo

}*/
